#include<iostream>
using namespace std;

void removeOcc(string str,string part){     //part abc--> in dabcd
    while(str.length() != 0 && str.find(part)<str.length){
        str.erase(str.find(part),part.length);
    }
    return s;
}



int main()
{
 
 
    return 0;
}